import classes from "./CartButton.module.css";

const CartButton = (props) => {
  return (
    <button>
      <span>My Cart</span>
      <span></span>
    </button>
  );
};

export default CartButton;
